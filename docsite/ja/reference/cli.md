# CLI

このページで扱う予定の内容:

- `scripts/evolve.py`・`scripts/synopsize.py`・`scripts/narrate.py`・`scripts/random_baseline.py` の全引数
- `viewer/server.py` の起動引数（`--runs`・`--control`・`--host`・`--port`・`--repo`）

!!! note "準備中"
    正確な引数一覧は各スクリプトの `argparse` 定義（ソース: `scripts/`・`viewer/server.py`）を参照してください。
