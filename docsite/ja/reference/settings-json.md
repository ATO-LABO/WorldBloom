# settings.json

このページで扱う予定の内容:

- `output`（LLM バックエンド・`default_backend`・`gpu_guard`）の全項目
- `evolution`（並列数など）の全項目
- `.gitignore` 対象である理由（API キーを含み得るため）

!!! note "準備中"
    現時点の例は [docs/DEVELOPMENT.md](https://github.com/ATO-LABO/WorldBloom/blob/main/docs/DEVELOPMENT.md) にまとまっています。
