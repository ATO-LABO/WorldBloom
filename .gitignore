# Python
__pycache__/
*.pyc
.pytest_cache/

# Run outputs live in C:\Projects\WorldBloom-local (never in the repo)
runs/
*.parquet

# Snapshot backups taken before applying external-AI deliveries
_backup_before_*/

# Claude Code session state / internal workflow notes (not for public distribution)
.claude/
CLAUDE.md

# LLM settings (API keys)
settings.json

# Local docs site build output
_site/

# execution/provenance.py's directory_lock() marker file -- created as a
# side effect of any locked read/write under projects/<world>/patches/
# (approved patches, epoch chains), never meant to be tracked.
projects/**/.write.lock
