# Python
__pycache__/
*.pyc
.pytest_cache/

# Run outputs live in C:\Projects\WorldBloom-local (never in the repo)
runs/
*.parquet

# Snapshot backups taken before applying external-AI deliveries
_backup_before_*/

# Claude Code session state / internal workflow notes (not for public distribution)
.claude/
CLAUDE.md

# LLM settings (API keys)
settings.json

# Local docs site build output
_site/
